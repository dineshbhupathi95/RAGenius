# RAGenius
RAGenius is a Retrieval-Augmented Generation (RAG) based application that enables users to interact with documents through natural language queries. The system processes documents, generates embeddings, retrieves relevant context using a vector database, and leverages LLMs to provide accurate and context-aware responses.
